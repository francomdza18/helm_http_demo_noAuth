apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagina-demo.fullname" . }}
spec:
  selector:
    app: {{ include "pagina-demo.name" . }}
  ports:
    - name: 8080-tcp
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
    - name: 8443-tcp
      protocol: TCP
      port: 8443
      targetPort: 8443
