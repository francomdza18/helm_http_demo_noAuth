apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pagina-web.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "pagina-web.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "pagina-web.name" . }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html-volume
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html
            - name: html-volume
              mountPath: /usr/share/nginx/html/logo.png
              subPath: logo.png

      volumes:
        - name: html-volume
          configMap:
            name: {{ include "pagina-web.fullname" . }}-html

