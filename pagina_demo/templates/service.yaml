apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagina_demo.fullname" . }}
  labels:
    {{- include "pagina_demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pagina_demo.selectorLabels" . | nindent 4 }}
